"use client";

// Visual theme preset switching was intentionally removed:
// the app always uses the preset that is active in Admin > Personalização.

export default function ThemePresetSelector({ sidebarOpen }) {
    return null;
}
